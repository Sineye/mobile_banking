import 'package:flutter/material.dart';

class PasswordLoginScreen extends StatelessWidget {
  const PasswordLoginScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('Podaj hasło.')
    );
  }

}